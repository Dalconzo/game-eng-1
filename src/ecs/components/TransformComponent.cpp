#include "ecs/components/TransformComponent.h"
#include "ecs/Entity.h"

namespace Engine {
namespace ECS {

// Since all methods are defined in the header file, 
// there's not much to implement here.
// However, if we add more complex transformations in the future,
// their implementations would go here.

} // namespace ECS
} // namespace Engine
